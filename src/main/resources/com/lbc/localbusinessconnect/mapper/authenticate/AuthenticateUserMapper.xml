<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.lbc.localbusinessconnect.mapper.authenticate.AuthenticateUserMapper">

    <select id="authenticateUser" useCache="false" resultMap="authenticateUserMap">
        SELECT USERID, PASSWORD
        FROM USER_CREDENTIAL
        WHERE USERID=#{userName}
    </select>

    <resultMap id="authenticateUserMap" type="com.lbc.localbusinessconnect.model.request.UserCredentialRequest">
        <result column="USERID" property="userName" />
        <result column="PASSWORD" property="password" />
    </resultMap>

    <insert id="registerUserCredentials">
        INSERT INTO
        USER_CREDENTIAL (
            USERID,
            PASSWORD,
            ACCOUNTTYPE,
            ACCOUNTSTATUS,
            CREATEDDATE,
            LASTLOGIN
        ) VALUES (
            #{userCredentialDetails.userId},
            #{userCredentialDetails.password},
            #{userCredentialDetails.accountType},
            #{userCredentialDetails.accountStatus},
            SYSDATE,
            SYSDATE
        )
    </insert>
</mapper>